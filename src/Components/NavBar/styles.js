
// Navigation bar styles
const styles = theme => ({
  root: {
    flexGrow: 1
  },
  flex: {
    // Allows next icon/elemend in Navbar to 'float right'
    flexGrow: 1
  },
  menuButton: {
    marginLeft: -12,
    marginRight: 20,
  },
  alignRight: {
    alignItems: 'right'
  }
});

export default styles;